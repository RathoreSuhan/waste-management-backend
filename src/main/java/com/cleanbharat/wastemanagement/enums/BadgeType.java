package com.cleanbharat.wastemanagement.enums;

/**
 * Represents leaderboard badge
 * earned by a cleaner based on
 * total reward points.
 */
public enum BadgeType {

    // 0 - 199 reward points
    BRONZE,

    // 200 - 499 reward points
    SILVER,

    // 500+ reward points
    GOLD
}