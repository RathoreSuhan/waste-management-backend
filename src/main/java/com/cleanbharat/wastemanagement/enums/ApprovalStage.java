package com.cleanbharat.wastemanagement.enums;

public enum ApprovalStage {

    // Municipal decision on a cleanup proposal (before work starts)
    PROPOSAL,

    // Municipal decision on submitted cleanup evidence (after work)
    COMPLETION
}
